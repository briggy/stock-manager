<?php

class CategsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('categs')->truncate();

		$categs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('categs')->insert($categs);
	}

}
